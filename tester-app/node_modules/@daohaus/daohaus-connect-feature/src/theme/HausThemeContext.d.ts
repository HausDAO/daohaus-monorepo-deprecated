import { ReactNode } from 'react';
import { ReactSetter } from '@daohaus/common-utilities';
import { Theme } from '../types/theming';
import './global/fonts.css';
import { CustomToastProps } from '../types/toast.types';
declare type HausUI = {
    theme: Theme;
    setTheme: ReactSetter<Theme>;
    toggleLightDark: () => void;
    setToast: (toast: CustomToastProps) => void;
};
declare type ProviderProps = {
    children: ReactNode;
    defaultDark?: Theme;
    defaultLight?: Theme;
    startDark?: boolean;
};
export declare const HausThemeContext: import("react").Context<HausUI>;
export declare const HausThemeProvider: ({ children, defaultDark, defaultLight, startDark, }: ProviderProps) => JSX.Element;
export default HausThemeProvider;
