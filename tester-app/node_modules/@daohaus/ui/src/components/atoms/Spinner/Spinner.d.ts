declare type SpinnerType = {
    topColor?: string;
    bottomColor?: string;
    size?: string;
    strokeWidth?: string;
    margin?: string;
    padding?: string;
    speed?: string;
};
export declare const Spinner: ({ topColor, bottomColor, size, margin, padding, strokeWidth, speed, }: SpinnerType) => JSX.Element;
export {};
