import { ValidNetwork } from '@daohaus/common-utilities';
import { AccountProfile } from '@daohaus/common-utilities';
declare type MemberCardProps = {
    profile: AccountProfile;
    explorerNetworkId: ValidNetwork;
    minWidth?: string;
    menuBg?: string;
    className?: string;
};
export declare const MemberCard: ({ className, profile, explorerNetworkId, minWidth, }: MemberCardProps) => JSX.Element;
export {};
