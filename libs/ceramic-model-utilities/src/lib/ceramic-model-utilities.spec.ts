import { ceramicModelUtilities } from './ceramic-model-utilities';

describe('ceramicModelUtilities', () => {
  it('should work', () => {
    expect(ceramicModelUtilities()).toEqual('ceramic-model-utilities');
  });
});
