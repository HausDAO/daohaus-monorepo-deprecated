export declare type ProgressProps = {
    backgroundColor: string;
    progressSection: {
        percentage: string;
        color: string;
    }[];
};
export declare const Progress: ({ backgroundColor, progressSection, }: ProgressProps) => JSX.Element;
