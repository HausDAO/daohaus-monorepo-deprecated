import { Field, OptionType, Buildable } from '../../../types';
declare type TimePickerProps = Field & {
    defaultValue?: string;
    options?: OptionType[];
    selectId?: string;
    selectPlaceholder?: string;
};
export declare const TimePicker: ({ id, options, selectId, rules, ...props }: Buildable<TimePickerProps>) => JSX.Element;
export {};
