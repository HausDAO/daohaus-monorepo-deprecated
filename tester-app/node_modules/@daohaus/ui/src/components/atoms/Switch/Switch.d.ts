import React, { FocusEventHandler, FormEventHandler } from 'react';
import { SwitchProps } from '@radix-ui/react-switch';
export declare type Props = {
    defaultOn?: boolean;
    fieldLabel: string;
    id?: string;
    className?: string;
    switchOn?: boolean;
    onBlur?: FocusEventHandler<HTMLButtonElement>;
    onChange?: FormEventHandler<HTMLButtonElement>;
    onCheckedChange?: () => void;
};
export declare const Switch: React.ForwardRefExoticComponent<Omit<SwitchProps, "defaultChecked" | "checked"> & Props & React.RefAttributes<HTMLInputElement>>;
