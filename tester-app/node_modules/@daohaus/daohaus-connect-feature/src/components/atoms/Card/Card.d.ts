import { ReactNode } from 'react';
export declare type CardProps = {
    children: ReactNode;
    className?: string;
    success?: boolean;
    warning?: boolean;
    error?: boolean;
    width?: string;
};
export declare const Card: ({ className, width, children, }: CardProps) => JSX.Element;
