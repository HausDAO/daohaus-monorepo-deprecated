import { ReactNode } from 'react';
import { Buildable } from '../../../types/formAndField';
declare type CheckGateProps = {
    gateLabel: string;
    fields: Array<ReactNode>;
    onUnchecked?: () => void;
};
export declare const CheckGate: ({ id, fields, gateLabel, onUnchecked, rules, ...props }: Buildable<CheckGateProps>) => JSX.Element;
export default CheckGate;
