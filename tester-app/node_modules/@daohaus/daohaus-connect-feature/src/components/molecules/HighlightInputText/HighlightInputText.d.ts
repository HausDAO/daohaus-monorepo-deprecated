import { Buildable } from '../../../types/formAndField';
declare type HighlightTextProps = {
    color?: string;
    description: string;
    highlightColor?: string;
    highlightInputId?: string;
};
export declare const HighlightInputText: ({ color, description, highlightColor, highlightInputId, }: Buildable<HighlightTextProps>) => JSX.Element;
export {};
