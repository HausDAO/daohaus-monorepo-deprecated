declare type DataIndicatorProps = {
    label?: string;
    data?: string | number;
    size?: 'lg' | 'sm';
    info?: string;
};
export declare const DataIndicator: ({ label, data, size, info, }: DataIndicatorProps) => JSX.Element;
export {};
