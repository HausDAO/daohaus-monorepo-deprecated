export type TemporaryDAOType = {
  isDelegate: boolean;
  daoName: string;
  members: number;
  vaults: number;
  tokenSymbol: string;
  activeProposals: number;
  totalProposals: number;
  power: number;
  network: string;
  contractName: string;
};
export type ListType = 'cards' | 'table';
