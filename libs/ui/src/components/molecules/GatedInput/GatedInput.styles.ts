import styled from 'styled-components';
import { field } from '../../../theme/component/fieldFamily';

export const GatedInputBox = styled.div`
  display: box;
  width: 100%;
  max-width: ${field.size.md};
`;