/// <reference types="react" />
export declare type AvatarProps = {
    src?: string;
    alt?: string;
    fallback?: string | React.ReactNode;
    delayMs?: number;
    size?: string;
    className?: string;
};
export declare const Avatar: ({ src, size, alt, delayMs, fallback, className, }: AvatarProps) => JSX.Element;
