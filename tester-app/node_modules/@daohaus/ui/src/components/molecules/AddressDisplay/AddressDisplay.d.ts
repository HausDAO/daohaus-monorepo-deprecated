import { Keychain } from '@daohaus/common-utilities';
declare type AddressDisplayProps = {
    address: string;
    explorerNetworkId?: keyof Keychain;
    copy?: boolean;
    truncate?: boolean;
    txHash?: boolean;
    textOverride?: string;
    className?: string;
};
export declare const AddressDisplay: ({ address, explorerNetworkId, copy, truncate, txHash, textOverride, className, ...props }: AddressDisplayProps) => JSX.Element;
export {};
