import React from 'react';
import { IconType } from 'react-icons';
interface IApp {
    name: string;
    url: string;
    icon?: IconType | React.FunctionComponent<React.SVGProps<SVGSVGElement> & {
        title?: string | undefined;
    }>;
}
export declare type AppSwitcherProps = {
    trigger: IApp;
    apps: IApp[];
    spacing?: string;
    width?: string;
    menuBg?: string;
    className?: string;
};
export declare const AppSwitcher: ({ className, trigger, apps, width, }: AppSwitcherProps) => JSX.Element;
export {};
