import React from 'react';
import { DropdownMenuProps, DropdownMenuContentProps } from '@radix-ui/react-dropdown-menu';
export declare type DropdownItem = {
    content: React.ReactNode;
    key?: string;
};
declare type DropdownProps = DropdownMenuProps & DropdownMenuContentProps & {
    trigger: React.ReactNode;
    spacing?: string;
    menuMinWidth?: string;
    menuBg?: string;
    className?: string;
};
export declare const Dropdown: ({ defaultOpen, open, onOpenChange, modal, dir, trigger, side, sideOffset, align, alignOffset, avoidCollisions, className, menuMinWidth, menuBg, children, }: DropdownProps) => JSX.Element;
export {};
